下面整理成 8 段，可直接分别保存为 `narration_000` 到 `narration_007`。

---

### narration_000.txt

```text id="n00001"
先看题目。
已知菱形边长为5，tanB等于2。
沿DE折叠后，点C对应到C'。
要求C'到BC的距离。
这题我们用坐标法来做。
```

---

### narration_001.txt

```text id="n00002"
先作高。
过A向BC作垂线，垂足为F。
由 tanB = AF / BF = 2，
设 BF = x，则 AF = 2x。
再用勾股定理，
解得 AF = 2√5。
```

---

### narration_002.txt

```text id="n00003"
建立坐标。
取B为原点，令BC在x轴上。
于是 B(0,0)，C(5,0)。
再根据刚求出的高，
可得 A(√5, 2√5)，D(5+√5, 2√5)。
所以 AB 的方程是 y = 2x。
```

---

### narration_003.txt

```text id="n00004"
接着求点E。
折叠轴DE平分角 BEB'。
已知这个角是90度，
所以角 BED 是45度。
再结合E在AB上，
联立方程，
求出点E的坐标。
```

---

### narration_004.txt

```text id="n00005"
现在求 BE。
因为B和E的坐标都已知，
直接套距离公式。
计算后得到，
BE = 5 - √5。
```

---

### narration_005.txt

```text id="n00006"
再求点C'。
点C'是点C关于直线DE的对称点。
所以把点C代入对称公式。
就能得到
C' 的坐标。
```

---

### narration_006.txt

```text id="n00007"
最后求距离。
因为BC在x轴上，
所以C'到BC的距离，
就是C'纵坐标的绝对值。
结果是 3√5。
答案选D。
```

---

### narration_007.txt

```text id="n00008"
总结一下。
这题的关键是：
先求高，
再建立坐标，
最后把折叠转成对称来计算。
```
